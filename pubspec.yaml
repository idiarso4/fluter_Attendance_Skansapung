name: absensi_app
description: Aplikasi Absensi SMKN 1 Punggelan
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get: ^4.6.5
  dio: ^5.3.2
  get_storage: ^2.1.1
  image_picker: ^1.0.4
  geolocator: ^10.1.0
  intl: ^0.18.1
  camera: ^0.10.5+4
  permission_handler: ^11.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
